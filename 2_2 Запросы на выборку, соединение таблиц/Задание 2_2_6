SELECT
    name_author,
    SUM(amount) AS Количество
FROM
    author LEFT JOIN book
    ON author.author_id = book.author_id
GROUP BY name_author
HAVING SUM(book.amount) < 10
       OR SUM(book.amount) IS NULL
ORDER BY Количество;